# v1.2 known gaps

Honest scope-versus-shipped accounting. Every item below was
identified during the v1.2 development cycle, considered, and
**deliberately deferred** for a documented reason. None are silent
bugs; each names the environment / work needed to close it.

This list is the difference between "code-feature-complete" (which
v1.2.0 is) and "battle-tested across the variety of AD environments
a tool like this needs to handle" (which it isn't yet).

---

## 1. Real-domain validation at scale (only single small domain tested)

**Status:** v1.2.0 was field-validated against ONE Server 2019 lab
domain (~10 users, single child of a 4-domain forest). That single
test surfaced 7 silent-failure field bugs (PRs #29–#35) — extrapolate
to estates kerb-map hasn't seen.

**What's untested:**
- Server 2022 / 2025 forests (BadSuccessor module is the only Server
  2025 surface; not exercised against a real Server 2025 DC)
- 5k+ user estates — paging behaviour, throughput, memory
- Multi-DC sites with referrals
- Hardened estates (signing required, channel binding, restricted RPC)
- Estates with weird schema extensions (Exchange, ConfigMgr, custom)
- Read-only DCs (RODC) — UAC bit 0x4000000 — uncertain if scan adapts

**What it would take:** access to 3–5 different forest topologies
(small / mid / hardened / multi-domain / Server 2025) and an operator
willing to run kerb-map against each + report bugs. Each forest will
likely surface 3–10 bugs the lab didn't, based on the v1.2 ratio.

---

## 2. Real BloodHound CE 5.x ingest validation ✅ **closed**

**Status:** Validated end-to-end against a running BH CE 5.x docker
compose stack (lab Samba 4 DC, 1500 stub users + 3 computers + 37
groups). All 4 standard JSONs (users / computers / groups / domains)
ingest cleanly, AND KerbMap findings now fold into the target
node's `Aces` array so they render as **native BH CE edges**:

```cypher
MATCH (s)-[r:GetChanges|GetChangesAll]->(d:Domain) RETURN s.name
// → SVC_OLD_ADMIN@LAB.LOCAL (full DCSync = both edges present)

MATCH (s)-[r:AddKeyCredentialLink]->(t:User) RETURN s.name, t.name
// → HELPDESK_OP → BOB_DA (seeded Shadow Creds path)
// → ACCOUNT OPERATORS → BOB_DA / ADMINISTRATOR / ADMIN_ORPHAN

MATCH (s)-[r:WriteDacl]->(t:Group {name:'DOMAIN ADMINS@LAB.LOCAL'})
RETURN s.name
// → ACCOUNT OPERATORS@LAB.LOCAL (Tier-0 ACL takeover, 1 hop)

MATCH p=shortestPath((s:Group {name:'ACCOUNT OPERATORS@LAB.LOCAL'})
                     -[*1..3]->(t:User {name:'BOB_DA@LAB.LOCAL'}))
RETURN length(p)
// → 1 (direct edge — operator sees the Tier-0 path immediately)
```

**Field bugs surfaced and fixed during validation:**

1. The previous shape — a top-level `kerbmap_edges.json` with
   `meta.type="kerbmap_edges"` — caused BH CE to reject the *entire
   upload* with HTTP 500 + "no valid meta tag found". BH CE 5.x's
   ingester only accepts the canonical SharpHound types. Fixed by
   renaming to `_kerbmap_metadata.json` (underscore prefix → BH CE
   skips during ingest).
2. The sidecar approach above made the zip ingestible but left
   findings invisible to the BH CE UI. Fixed by folding three
   finding classes — DCSync, Shadow Creds, Tier-0 ACL — into the
   target node's `Aces` array with SharpHound-recognised
   `RightName` (`GetChanges`/`GetChangesAll`/`AddKeyCredentialLink`/
   `GenericAll`/`WriteDacl`/`WriteOwner`/`GenericWrite`/`AddMember`/
   `AddSelf`). Each sidecar entry is annotated with `folded: bool`
   so kerb-chain knows which findings render in the graph.

**What remains as v1.3 work** (not blocking — the high-impact
findings already fold):
- ADCS Extended (ESC4/5/7/9/13/15) → would need BH CE's `CertTemplate`
  / `EnterpriseCA` / `RootCA` node types in our collection
- BadSuccessor / OU computer-create → would need OU node enumeration
- Large graphs (50k+ nodes) — ingest time, query latency unmeasured

---

## 3. CI Python matrix (3.10/3.11/3.12) ✅ **closed**

Closed in this PR — `.github/workflows/test.yml` now runs the suite
on all three Python versions. Locally verified with `uv` first;
both 3.10 and 3.11 pass 599 tests with no code changes needed.

---

## 4. Coverage on `kerb_map/modules/` ✅ **closed**

Aggregate now at **84%+ on `kerb_map/modules/`**. Per-file:

- `hygiene_auditor.py` — **100%** covered (280/280 statements). 58 new
  unit tests covering all 10 sub-modules + a lab-integration test
  gated by `KERBMAP_LAB_DC_IP` that exercises the full `audit()`
  sweep against a running Samba 4 DC.

The lab integration is the same pattern that surfaced the 3 impacket
bugs in PR #41: pure mocks pass while real-traffic exposes shape
mismatches. It surfaced one real bug in `_credential_exposure` — the
LDAP filter `(description=*pass*|*pwd*|*cred*)` and the regex set
(`pass`, `pwd`, `cred`, `secret`, `pin`) both missed the bare `pw=`
abbreviation that ops actually use ("SQL svc — pw=Spring2024!"); both
layers updated to also catch `pw=` / `secret=` / `key=` / `pin=`.

---

## 5. kerb-chain orchestrator (separate repo, not touched this cycle)

**Status:** A `kerb-chain` package is mentioned in earlier session
notes as the orchestration / playbook layer that would consume
kerb-map's JSON output and chain attacks. **No work landed on it
this cycle.**

**What's missing:** unclear without inspecting that repo. Likely
needs:
- Wire it to consume kerb-map's `-o json` output as input
- Add playbook entries for the new v2 plugin findings
  (DCSync rights, Shadow Creds, Tier-0 ACL, OU computer-create,
  ADCS Extended ESC4-15, etc.)
- Add no-creds entrypoints (timeroast, spray)
- Update for the substituted-placeholder convention

**What it would take:** a separate session focused on the kerb-chain
repo, with the user able to point at the current state and the
intended playbook surface.

---

## 6. Lab provision validation ✅ **closed**

`vagrant up` validated end-to-end on Arch (vagrant 2.4.9 from AUR +
VirtualBox 7.2 + Ubuntu jammy64 base box). Provisioning surfaces 4
real bugs in the lab files which are now fixed:

1. **`ldb-tools` missing** from `provision_dc.sh` apt install. Without
   it, every `ldbmodify` call in the seed silently failed — meaning
   no UAC bit toggling, no key-credential blob, no DCSync ACE seeded.
   kerb-map would then scan a clean lab and find nothing.
2. **`Smith, John` username** rejected by samba (sAMAccountName can't
   contain commas) AND `--surname='Smith, of Sendai'` produced an
   invalid DN with a comma in CN. Dropped — DN-escape testing belongs
   in unit tests, not the lab seed.
3. **`set -e` interaction** with the SID-decoding pipeline (`ldap |
   sed | base64 | python`). On idempotent re-runs the python returned
   empty, the substitution exited non-zero, and `set -e` bailed the
   whole script. Removed `-e` (kept `-uo pipefail`); script is
   designed for partial failure (every line uses `|| true`).
4. **SID decoder Python** raised `IndexError: index out of range` on
   empty objectSid output. Added a length guard.

After fixes: `vagrant provision dc01` runs the full seed end-to-end
and prints `[seed] complete at ...`. Every seeded object verified
present via `vagrant ssh dc01 -c 'samba-tool user list'` /
`samba-tool computer list`.

**One known follow-up surfaced during this validation, NOT closed:**
running kerb-map against the seeded Samba 4 DC fails with
`strongerAuthRequired` on every transport (LDAPS / StartTLS / SASL /
plain). Modern Samba 4 requires LDAP signing for SASL binds, and
ldap3's Kerberos SASL binding doesn't enable signing flags by
default. This is the "hardened estate" gap (#1 below) surfacing in
the lab — kerb-map can scan against the loose Server 2019 in the
culling-game lab but not against signing-required Samba. Fix needs
ldap3 SASL binding to set `sasl_security="sign"` or equivalent.
Tracked as a v1.2.x follow-up.

**Lab is now reproducible** by anyone with vagrant + virtualbox:

```bash
sudo pacman -S vagrant virtualbox  # or apt install equivalent
cd lab && vagrant up
# Provisioning takes ~5–10 min on first run (apt install + samba
# provision + 1500 stub users). On subsequent boots / `vagrant
# provision`, only the idempotent seed runs (~30 s).
```

---

## 7. Performance baseline (partial)

**Status:** First numbers landed against the seeded Samba 4 lab
(1500 stub users, 3 computers, 37 groups, ~30 ACE-bearing objects):

| Profile           | Wall time | Output zip |
|-------------------|----------:|-----------:|
| `--all` (legacy)  |     3.6 s |       60 MB |
| `--all --v2`      |    10.2 s |      113 MB |

`--v2` adds 2.8× wall time and 1.9× output size on this lab — driven
by the 6 ACL-walking modules (DCSync rights, Shadow Creds, Tier-0 ACL,
delegation, OU computer-create, ADCS Extended) each issuing
`get_security_descriptor=True` LDAP queries against ~hundreds of
objects. Output growth is dominated by per-finding metadata in JSON.

**What's still unmeasured:**
- Scan time on a 5k+ user *real* estate (lab is 1500 stubs, no real
  ACE diversity)
- RAM peak on a forest with 50+ GPOs
- LDAP query count per scan (rough estimate from code: ~20 queries
  legacy + ~10 per v2 module = ~80–100 queries per `--all --v2` scan)
- Time-to-first-finding (matters for `--quiet` operators wanting
  early signal)
- Whether the 2.8× v2 cost holds on real estates or balloons with
  ACE-rich objects

**What it would take:** access to a 5k+ user lab + Python's
`tracemalloc` / `psutil` instrumentation. Half a day of benchmarking
+ `docs/PERFORMANCE.md` write-up.

---

## 8. Detection profile validation

**Status:** `README.md`'s Detection Profile table is **estimated, not
measured**. Claims like "v2 plugins generate Event 1644 only" or
"Timeroast = NTP request anomaly only" are based on the LDAP query
shape and protocol-level reasoning, **not on watching a SOC analyst's
SIEM during a kerb-map run**.

**What's unmeasured:**
- Actual Win Event IDs generated per module
- MDI alert classes triggered
- EDR signatures hit
- Specific 4769 / 4625 / 5145 counts per scan

**What it would take:** sit alongside a SOC analyst with Splunk /
Sentinel / MDI open during a kerb-map run. A 30-minute coordinated
run would produce enough signal to update the table with measured
values + IDs.

---

## 9. GPP SMB-grep plumbing ✅ **closed**

`CVEBase` now carries optional `username` / `password` / `nthash` /
`use_kerberos` (keyword-only — existing 3-arg construction sites
unchanged). `gpp_passwords.py` opens an SMB session against SYSVOL
via impacket's `SMBConnection`, walks `Policies\{GUID}\` subtrees,
matches GPP XML filenames case-insensitively, decrypts `cpassword=`
with the MS-published AES-256 key (PKCS#7-padded, UTF-16-LE),
extracts the sibling `userName=`, and surfaces CRITICAL with the
cleartext credential.

End-to-end validated against the lab's seeded GPP cpassword
(`Groups.xml` under the Default Domain Policy with `Password1!`
encrypted via the public key — see `lab/seed_vulnerabilities.sh`):

```
CRITICAL  GPP Passwords (cpassword) (MS14-025)
          Found 1 cleartext password(s) in GPP XML on SYSVOL.
          Sample: user='helpdesk_admin', cleartext=***.
          patch_status: confirmed vulnerable via SMB-grep
```

Without credentials (kerberos-only without GSSAPI-on-SMB plumbing,
or anonymous bind) the check falls back to the prior INDETERMINATE
behaviour from PR #30 — listing the GPOs and pointing the operator
at smbclient / Get-GPPPassword / impacket recipes.

Three field bugs surfaced and fixed during validation:
1. impacket's `openFile` + `readFile` take `treeId` (numeric, from
   `connectTree`) — passing the share name string returned zero
   bytes silently. Fixed by using the higher-level `getFile` helper
   that takes share name directly.
2. The lab's Samba provision creates `MACHINE/` (uppercase) under
   each GPO; the original seed wrote to `Machine/` (mixed case),
   and Linux's case-sensitive filesystem made these two distinct
   directories. Fixed by writing to the existing MACHINE.
3. The username-extraction regex matched `newName=""` (which appears
   earlier in the GPP attribute list) instead of `userName=...`.
   Fixed by ranking patterns and skipping empty matches.

---

## 10. Operator iteration loop (not run yet)

**Status:** v1.2.0 is shipping with a single round of field
validation by one operator (the development session). Real
engagements surface dozens of rough edges per week — confusing
error messages, unexpected ACE shapes, weird LDAP referral chains,
AD bugs that crash impacket, environments where Python 3.x has a
quirk. **None of that has happened yet.**

**What it would take:** ship v1.2.0 to 3–5 trusted operators, gather
issues over 2–4 weeks, triage and patch. The bug volume should taper
as edge cases get fixed; expect another 5–10 PRs of the
PR-#29-through-#35 calibre.

---

## Roadmap

A reasonable v1.2.x → v1.3 trajectory closes these in roughly this
order:

1. **v1.2.1** — operator iteration patches (#10), dMSA / schema-
   tolerance pattern extended to other Server 2025-only classes
   (msDS-AuthN-PolicySilo, etc.).
2. **v1.2.2** — operator iteration patches as they come in (#10).
3. **v1.3.0** — kerb-chain integration (#5) + cross-domain trust
   walking + BH CE node enumeration for ADCS / OU / dMSA so the
   remaining KerbMap finding classes can also fold into Aces (#2
   v1.3 follow-up) + performance baseline at scale (#7 remainder).
4. Out-of-band: lab provision validation (#6) and detection profile
   measurement (#8) — both depend on hardware / coordination, not
   code.
